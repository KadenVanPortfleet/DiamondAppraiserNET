% import data set

data = readmatrix('diamonds.csv');

% rotate data set so that each item has variable in columns.
data = rot90(data, -1);

% Get target values from dataset
targets = data(7,:);

dataPrep = [ data(1,:); data(2,:); data(3,:); data(4,:); data(5,:); data(6,:); data(8,:); data(9,:); 